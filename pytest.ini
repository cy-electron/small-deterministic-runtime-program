[pytest]
# Keep temporary test files inside this project.  Some Windows installations
# restrict the default %TEMP% pytest directory, which otherwise makes the
# JSON-export test fail before it starts.
addopts = -p no:cacheprovider --basetemp=artifacts/pytest_tmp
